# Copy to .env.test and adjust if needed. This file is gitignored.
# E2E tests run against this database — keep it separate from dev/prod.

DATABASE_URL="postgresql://flowpay:change_me@localhost:5433/flowpay_test?schema=public"
JWT_SECRET="replace-with-a-64+-char-random-string"
JWT_EXPIRES_IN="1d"
NODE_ENV="development"
